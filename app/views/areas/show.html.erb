<p id="notice"><%= notice %></p>

<p>
  <strong>地域:</strong>
  <%= @area.name %>
<% if $sc_switch %>
  <strong>SC:</strong>
  <%= @area.service_centers.map{|service_center| service_center.name}*", " %>
<% end %>
</p>

<%= link_to '編集', edit_area_path(@area) %> |
<%= link_to '戻る', areas_path %>
